import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Providers from "./providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PriceIQ — See the real cost of getting paid",
  description:
    "PriceIQ helps you understand the true cost of getting paid. Model fees, FX, platform costs, and margins with clarity.",
  openGraph: {
    title: "PriceIQ — See the real cost of getting paid",
    description:
      "Understand payment fees, FX costs, and margins with PriceIQ. Model real-world pricing and revenue scenarios.",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable}`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
